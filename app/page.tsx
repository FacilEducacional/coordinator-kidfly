"use client";
import { useState, useEffect, useRef } from "react";
import { supabase } from "./supabase";

var COLORS = { blue: "#2C5AA0", blueDark: "#1E3F73", blueLight: "#E8EFF8", pink: "#FE4F6B", pinkLight: "#FFF0F2", yellow: "#FFB800", yellowLight: "#FFF8E6", green: "#2ECC71", greenLight: "#EAFAF1", white: "#FFFFFF", bg: "#F5F7FB", text: "#1A2332", textMuted: "#6B7A90", border: "#E2E8F0" };

function StatusBadge({ status }: { status: string }) { var c: any = { pendente: { bg: COLORS.yellowLight, color: "#B8860B", label: "Pendente" }, aprovado: { bg: COLORS.greenLight, color: "#1B8A4A", label: "Aprovado" }, revisar: { bg: COLORS.pinkLight, color: COLORS.pink, label: "Revisar" } }; var s = c[status] || c["pendente"]; return <span style={{ background: s.bg, color: s.color, padding: "4px 10px", borderRadius: 20, fontSize: 12, fontWeight: 600 }}>{s.label}</span>; }

function SuccessMessage({ message, onClose }: { message: string; onClose: () => void }) { return (<div style={{ position: "fixed", top: 0, left: 0, right: 0, bottom: 0, zIndex: 200, background: "rgba(0,0,0,0.4)", display: "flex", alignItems: "center", justifyContent: "center", padding: 24 }}><div style={{ background: COLORS.white, borderRadius: 20, padding: "32px 24px", textAlign: "center", maxWidth: 320, width: "100%" }}><div style={{ fontSize: 48, marginBottom: 12 }}>&#9989;</div><h3 style={{ fontSize: 18, fontWeight: 700, color: COLORS.text, margin: "0 0 8px" }}>{message}</h3><button onClick={onClose} style={{ width: "100%", padding: 12, borderRadius: 12, border: "none", background: COLORS.blue, color: COLORS.white, fontSize: 14, fontWeight: 600, cursor: "pointer" }}>Ok</button></div></div>); }

function ViewEventModal({ aviso, destinatarios, usuarios, onClose }: any) { var di = destinatarios.filter(function(d: any) { return d.aviso_id === aviso.id; }); var isTodos = di.some(function(d: any) { return d.todos; }); var au = di.filter(function(d: any) { return d.usuario_id; }).map(function(d: any) { var u = usuarios.find(function(u2: any) { return u2.id === d.usuario_id; }); return u ? u.nome : "?"; }); return (<div style={{ position: "fixed", top: 0, left: 0, right: 0, bottom: 0, zIndex: 200, background: "rgba(0,0,0,0.4)", display: "flex", alignItems: "center", justifyContent: "center", padding: 24 }}><div style={{ background: COLORS.white, borderRadius: 20, padding: 20, maxWidth: 380, width: "100%" }}><h3 style={{ fontSize: 18, fontWeight: 700, color: COLORS.text, margin: "0 0 4px" }}>{aviso.titulo}</h3>{aviso.urgente && <span style={{ fontSize: 11, background: COLORS.pinkLight, color: COLORS.pink, padding: "2px 8px", borderRadius: 6, fontWeight: 600 }}>Urgente</span>}<div style={{ marginTop: 16, marginBottom: 12 }}><div style={{ fontSize: 13, fontWeight: 600, color: COLORS.text, marginBottom: 4 }}>Data</div>{aviso.data_evento ? <div style={{ fontSize: 14, color: COLORS.blue, fontWeight: 600, background: COLORS.blueLight, padding: "8px 12px", borderRadius: 10, display: "inline-block" }}>&#128197; {new Date(aviso.data_evento + "T12:00:00").toLocaleDateString("pt-BR")}</div> : <div style={{ fontSize: 13, color: COLORS.textMuted }}>Sem data</div>}</div><div style={{ marginBottom: 16 }}><div style={{ fontSize: 13, fontWeight: 600, color: COLORS.text, marginBottom: 4 }}>Responsaveis</div>{isTodos ? <span style={{ fontSize: 13, color: COLORS.green, fontWeight: 600, background: COLORS.greenLight, padding: "8px 12px", borderRadius: 10 }}>Todos</span> : au.length > 0 ? <div style={{ display: "flex", flexWrap: "wrap", gap: 6 }}>{au.map(function(n: string, i: number) { return <span key={i} style={{ fontSize: 12, background: COLORS.blueLight, color: COLORS.blue, padding: "4px 10px", borderRadius: 8, fontWeight: 600 }}>{n}</span>; })}</div> : <span style={{ fontSize: 13, color: COLORS.textMuted }}>Nenhum</span>}</div><button onClick={onClose} style={{ width: "100%", padding: 12, borderRadius: 12, border: "none", background: COLORS.blue, color: COLORS.white, fontSize: 14, fontWeight: 600, cursor: "pointer" }}>Fechar</button></div></div>); }

function LoginScreen({ onLogin }: { onLogin: (user: any) => void }) {
  var _a = useState(""); var email = _a[0]; var setEmail = _a[1]; var _b = useState(""); var pass = _b[0]; var setPass = _b[1]; var _c = useState(""); var erro = _c[0]; var setErro = _c[1]; var _d = useState(false); var ld = _d[0]; var setLd = _d[1];
  async function login() { setErro(""); setLd(true); var r = await supabase.from("usuarios").select("*, escolas(nome)").eq("email", email).eq("senha_hash", pass).single(); setLd(false); if (r.error || !r.data) { setErro("E-mail ou senha incorretos"); return; } onLogin(r.data); }
  return (<div style={{ minHeight: "100vh", background: "linear-gradient(145deg, " + COLORS.blue + " 0%, " + COLORS.blueDark + " 100%)", display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", padding: 24 }}>
    <div style={{ textAlign: "center", marginBottom: 40 }}><div style={{ width: 72, height: 72, borderRadius: 20, background: "rgba(255,255,255,0.15)", display: "flex", alignItems: "center", justifyContent: "center", margin: "0 auto 16px" }}><span style={{ fontSize: 36 }}>&#128203;</span></div><h1 style={{ color: COLORS.white, fontSize: 28, fontWeight: 700, margin: "0 0 4px" }}>Coordinator</h1><p style={{ color: "rgba(255,255,255,0.7)", fontSize: 14, margin: 0, letterSpacing: 1.5, textTransform: "uppercase", fontWeight: 500 }}>by KidFly</p></div>
    <div style={{ background: COLORS.white, borderRadius: 24, padding: "32px 24px", width: "100%", maxWidth: 380 }}>
      <h2 style={{ fontSize: 20, fontWeight: 700, color: COLORS.text, margin: "0 0 4px" }}>Bem-vinda! &#128075;</h2><p style={{ fontSize: 14, color: COLORS.textMuted, margin: "0 0 24px" }}>Entre com seu acesso KidFly</p>
      {erro && <div style={{ background: COLORS.pinkLight, color: COLORS.pink, padding: "10px 14px", borderRadius: 12, fontSize: 13, fontWeight: 600, marginBottom: 16 }}>{erro}</div>}
      <div style={{ marginBottom: 16 }}><label style={{ fontSize: 13, fontWeight: 600, color: COLORS.text, display: "block", marginBottom: 6 }}>E-mail</label><input type="email" value={email} onChange={function(e) { setEmail(e.target.value); }} placeholder="seu@email.com" style={{ width: "100%", padding: "12px 16px", borderRadius: 12, border: "2px solid " + COLORS.border, fontSize: 15, outline: "none", boxSizing: "border-box" }} /></div>
      <div style={{ marginBottom: 24 }}><label style={{ fontSize: 13, fontWeight: 600, color: COLORS.text, display: "block", marginBottom: 6 }}>Senha</label><input type="password" value={pass} onChange={function(e) { setPass(e.target.value); }} placeholder="********" style={{ width: "100%", padding: "12px 16px", borderRadius: 12, border: "2px solid " + COLORS.border, fontSize: 15, outline: "none", boxSizing: "border-box" }} /></div>
      <button onClick={login} disabled={ld} style={{ width: "100%", padding: "14px", borderRadius: 14, border: "none", background: "linear-gradient(135deg, " + COLORS.pink + " 0%, #E8405A 100%)", color: COLORS.white, fontSize: 16, fontWeight: 700, cursor: "pointer", opacity: ld ? 0.7 : 1 }}>{ld ? "Entrando..." : "Entrar"}</button>
      <p style={{ textAlign: "center", fontSize: 11, color: COLORS.textMuted, marginTop: 12, background: COLORS.blueLight, padding: "8px", borderRadius: 8 }}>Demo: maria@escola.com / 123456</p>
    </div>
  </div>);
}

function GestaoScreen({ user, turmas, usuarios, onUpdate }: any) {
  var _a = useState(""); var msg = _a[0]; var setMsg = _a[1];
  var _b = useState(false); var showAddTurma = _b[0]; var setShowAddTurma = _b[1];
  var _c = useState(""); var novaTurma = _c[0]; var setNovaTurma = _c[1];
  var _d = useState(0); var novaQtd = _d[0]; var setNovaQtd = _d[1];
  var _e = useState(""); var novaTurmaProf = _e[0]; var setNovaTurmaProf = _e[1];
  var _f = useState(false); var showAddProf = _f[0]; var setShowAddProf = _f[1];
  var _g = useState(""); var profNome = _g[0]; var setProfNome = _g[1];
  var _h = useState(""); var profEmail = _h[0]; var setProfEmail = _h[1];
  var _i = useState("coordenadora"); var profPapel = _i[0]; var setProfPapel = _i[1];
  var _j = useState<any>(null); var editTurma = _j[0]; var setEditTurma = _j[1];
  var _k = useState(""); var editTurmaNome = _k[0]; var setEditTurmaNome = _k[1];
  var _l = useState(0); var editTurmaQtd = _l[0]; var setEditTurmaQtd = _l[1];
  var _m = useState(""); var editTurmaProf = _m[0]; var setEditTurmaProf = _m[1];

  var profs = usuarios.filter(function(u: any) { return u.papel === "professora"; });
  var escolaNome = user.escolas?.nome || "Minha Escola";

  async function addTurma() {
    if (!novaTurma.trim()) return;
    var d: any = { escola_id: user.escola_id, nome: novaTurma, qtd_alunos: novaQtd };
    if (novaTurmaProf) d.professora_id = novaTurmaProf;
    await supabase.from("turmas").insert(d);
    setNovaTurma(""); setNovaQtd(0); setNovaTurmaProf(""); setShowAddTurma(false);
    setMsg("Turma adicionada!"); onUpdate();
  }

  async function saveTurma() {
    if (!editTurma || !editTurmaNome.trim()) return;
    var d: any = { nome: editTurmaNome, qtd_alunos: editTurmaQtd, professora_id: editTurmaProf || null };
    await supabase.from("turmas").update(d).eq("id", editTurma.id);
    setEditTurma(null); setMsg("Turma atualizada!"); onUpdate();
  }

  async function deleteTurma(id: string) {
    await supabase.from("turmas").delete().eq("id", id);
    setEditTurma(null); setMsg("Turma removida!"); onUpdate();
  }

  async function addProf() {
    if (!profNome.trim() || !profEmail.trim()) return;
    await supabase.from("usuarios").insert({ escola_id: user.escola_id, nome: profNome, email: profEmail, senha_hash: "123456", papel: profPapel });
    setProfNome(""); setProfEmail(""); setProfPapel("professora"); setShowAddProf(false);
    setMsg("Usuaria adicionada! Senha padrao: 123456"); onUpdate();
  }

  async function deleteUser(id: string) {
    await supabase.from("usuarios").delete().eq("id", id);
    setMsg("Usuaria removida!"); onUpdate();
  }

  function startEditTurma(t: any) {
    setEditTurma(t); setEditTurmaNome(t.nome); setEditTurmaQtd(t.qtd_alunos || 0); setEditTurmaProf(t.professora_id || "");
  }

  return (
    <div>
      {msg && <SuccessMessage message={msg} onClose={function() { setMsg(""); }} />}

      {editTurma && (<div style={{ position: "fixed", top: 0, left: 0, right: 0, bottom: 0, zIndex: 200, background: "rgba(0,0,0,0.4)", display: "flex", alignItems: "center", justifyContent: "center", padding: 24 }}><div style={{ background: COLORS.white, borderRadius: 20, padding: 20, maxWidth: 380, width: "100%" }}>
        <h3 style={{ fontSize: 18, fontWeight: 700, color: COLORS.text, margin: "0 0 16px" }}>Editar turma</h3>
        <label style={{ fontSize: 13, fontWeight: 600, color: COLORS.text, display: "block", marginBottom: 6 }}>Nome</label>
        <input value={editTurmaNome} onChange={function(e) { setEditTurmaNome(e.target.value); }} style={{ width: "100%", padding: "10px 14px", borderRadius: 10, border: "2px solid " + COLORS.border, fontSize: 14, outline: "none", boxSizing: "border-box", marginBottom: 10 }} />
        <label style={{ fontSize: 13, fontWeight: 600, color: COLORS.text, display: "block", marginBottom: 6 }}>Qtd alunos</label>
        <input type="number" value={editTurmaQtd} onChange={function(e) { setEditTurmaQtd(Number(e.target.value)); }} style={{ width: "100%", padding: "10px 14px", borderRadius: 10, border: "2px solid " + COLORS.border, fontSize: 14, outline: "none", boxSizing: "border-box", marginBottom: 10 }} />
        <label style={{ fontSize: 13, fontWeight: 600, color: COLORS.text, display: "block", marginBottom: 6 }}>Professora</label>
        <select value={editTurmaProf} onChange={function(e) { setEditTurmaProf(e.target.value); }} style={{ width: "100%", padding: "10px 14px", borderRadius: 10, border: "2px solid " + COLORS.border, fontSize: 14, outline: "none", boxSizing: "border-box", marginBottom: 16, background: COLORS.white }}>
          <option value="">Nenhuma</option>
          {profs.map(function(p: any) { return <option key={p.id} value={p.id}>{p.nome}</option>; })}
        </select>
        <div style={{ display: "flex", gap: 8, marginBottom: 10 }}><button onClick={function() { setEditTurma(null); }} style={{ flex: 1, padding: 10, borderRadius: 10, border: "2px solid " + COLORS.border, background: "transparent", color: COLORS.text, fontSize: 14, fontWeight: 600, cursor: "pointer" }}>Cancelar</button><button onClick={saveTurma} style={{ flex: 1, padding: 10, borderRadius: 10, border: "none", background: COLORS.blue, color: COLORS.white, fontSize: 14, fontWeight: 600, cursor: "pointer" }}>Salvar</button></div>
        <button onClick={function() { deleteTurma(editTurma.id); }} style={{ width: "100%", padding: 10, borderRadius: 10, border: "2px solid " + COLORS.pink, background: "transparent", color: COLORS.pink, fontSize: 14, fontWeight: 600, cursor: "pointer" }}>&#128465; Remover turma</button>
      </div></div>)}

      <h2 style={{ fontSize: 20, fontWeight: 700, color: COLORS.text, margin: "0 0 4px" }}>Gestao da Escola</h2>
      <p style={{ fontSize: 14, color: COLORS.textMuted, margin: "0 0 24px" }}>{escolaNome}</p>

      <div style={{ background: COLORS.white, borderRadius: 16, padding: 16, border: "1px solid " + COLORS.border, marginBottom: 16 }}>
        <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 12 }}>
          <h3 style={{ fontSize: 16, fontWeight: 700, color: COLORS.text, margin: 0 }}>Turmas ({turmas.length})</h3>
          <button onClick={function() { setShowAddTurma(!showAddTurma); }} style={{ background: COLORS.blue, color: COLORS.white, border: "none", borderRadius: 10, padding: "6px 14px", fontSize: 12, fontWeight: 600, cursor: "pointer" }}>{showAddTurma ? "X" : "+ Nova"}</button>
        </div>
        {showAddTurma && (<div style={{ background: COLORS.bg, borderRadius: 12, padding: 12, marginBottom: 12 }}>
          <input value={novaTurma} onChange={function(e) { setNovaTurma(e.target.value); }} placeholder="Nome da turma" style={{ width: "100%", padding: "8px 12px", borderRadius: 8, border: "2px solid " + COLORS.border, fontSize: 13, outline: "none", boxSizing: "border-box", marginBottom: 8 }} />
          <input type="number" value={novaQtd || ""} onChange={function(e) { setNovaQtd(Number(e.target.value)); }} placeholder="Qtd alunos" style={{ width: "100%", padding: "8px 12px", borderRadius: 8, border: "2px solid " + COLORS.border, fontSize: 13, outline: "none", boxSizing: "border-box", marginBottom: 8 }} />
          <select value={novaTurmaProf} onChange={function(e) { setNovaTurmaProf(e.target.value); }} style={{ width: "100%", padding: "8px 12px", borderRadius: 8, border: "2px solid " + COLORS.border, fontSize: 13, outline: "none", boxSizing: "border-box", marginBottom: 8, background: COLORS.white }}><option value="">Professora (opcional)</option>{profs.map(function(p: any) { return <option key={p.id} value={p.id}>{p.nome}</option>; })}</select>
          <button onClick={addTurma} style={{ width: "100%", padding: 8, borderRadius: 8, border: "none", background: COLORS.green, color: COLORS.white, fontSize: 13, fontWeight: 600, cursor: "pointer" }}>Adicionar turma</button>
        </div>)}
        {turmas.length === 0 && <p style={{ fontSize: 13, color: COLORS.textMuted }}>Nenhuma turma cadastrada</p>}
        {turmas.map(function(t: any) {
          return (<div key={t.id} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "10px 0", borderTop: "1px solid " + COLORS.border }}>
            <div><div style={{ fontSize: 14, fontWeight: 600, color: COLORS.text }}>{t.nome}</div><div style={{ fontSize: 12, color: COLORS.textMuted }}>{t.usuarios?.nome ? "Prof. " + t.usuarios.nome : "Sem professora"} - {t.qtd_alunos} alunos</div></div>
            <button onClick={function() { startEditTurma(t); }} style={{ padding: "4px 10px", borderRadius: 8, border: "none", background: COLORS.blueLight, fontSize: 11, fontWeight: 600, color: COLORS.blue, cursor: "pointer" }}>Editar</button>
          </div>);
        })}
      </div>

      <div style={{ background: COLORS.white, borderRadius: 16, padding: 16, border: "1px solid " + COLORS.border }}>
        <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 12 }}>
          <h3 style={{ fontSize: 16, fontWeight: 700, color: COLORS.text, margin: 0 }}>Equipe ({usuarios.length})</h3>
          <button onClick={function() { setShowAddProf(!showAddProf); }} style={{ background: COLORS.blue, color: COLORS.white, border: "none", borderRadius: 10, padding: "6px 14px", fontSize: 12, fontWeight: 600, cursor: "pointer" }}>{showAddProf ? "X" : "+ Nova"}</button>
        </div>
        {showAddProf && (<div style={{ background: COLORS.bg, borderRadius: 12, padding: 12, marginBottom: 12 }}>
          <input value={profNome} onChange={function(e) { setProfNome(e.target.value); }} placeholder="Nome completo" style={{ width: "100%", padding: "8px 12px", borderRadius: 8, border: "2px solid " + COLORS.border, fontSize: 13, outline: "none", boxSizing: "border-box", marginBottom: 8 }} />
          <input type="email" value={profEmail} onChange={function(e) { setProfEmail(e.target.value); }} placeholder="E-mail" style={{ width: "100%", padding: "8px 12px", borderRadius: 8, border: "2px solid " + COLORS.border, fontSize: 13, outline: "none", boxSizing: "border-box", marginBottom: 8 }} />
          <select value={profPapel} onChange={function(e) { setProfPapel(e.target.value); }} style={{ width: "100%", padding: "8px 12px", borderRadius: 8, border: "2px solid " + COLORS.border, fontSize: 13, outline: "none", boxSizing: "border-box", marginBottom: 8, background: COLORS.white }}><option value="professora">Professora</option><option value="coordenadora">Coordenadora</option><option value="diretora">Diretora</option></select>
          <button onClick={addProf} style={{ width: "100%", padding: 8, borderRadius: 8, border: "none", background: COLORS.green, color: COLORS.white, fontSize: 13, fontWeight: 600, cursor: "pointer" }}>Adicionar (senha: 123456)</button>
        </div>)}
        {usuarios.map(function(u: any) {
          var isCurrentUser = u.id === user.id;
          return (<div key={u.id} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "10px 0", borderTop: "1px solid " + COLORS.border }}>
            <div><div style={{ fontSize: 14, fontWeight: 600, color: COLORS.text }}>{u.nome} {isCurrentUser ? "(voce)" : ""}</div><div style={{ fontSize: 12, color: COLORS.textMuted }}>{u.email} - {u.papel}</div></div>
            {!isCurrentUser && <button onClick={function() { deleteUser(u.id); }} style={{ padding: "4px 10px", borderRadius: 8, border: "none", background: COLORS.pinkLight, fontSize: 11, fontWeight: 600, color: COLORS.pink, cursor: "pointer" }}>Remover</button>}
          </div>);
        })}
      </div>
    </div>
  );
}

function DashboardScreen({ user, avisos, planos, eventos, turmas }: any) {
  var nomeFirst = user.nome.split(" ")[0]; var escolaNome = user.escolas?.nome || "Minha Escola";
  var pendentes = planos.filter(function(p: any) { return p.status === "pendente"; }).length;
  var allEv = [...eventos, ...avisos.filter(function(a: any) { return a.data_evento; }).map(function(a: any) { return { id: "a-" + a.id, titulo: a.titulo, data: a.data_evento, cor: a.urgente ? COLORS.pink : COLORS.blue }; })].sort(function(a: any, b: any) { return a.data > b.data ? 1 : -1; });
  return (<div>
    <div style={{ marginBottom: 24 }}><h2 style={{ fontSize: 22, fontWeight: 700, color: COLORS.text, margin: "0 0 4px" }}>Bom dia, {nomeFirst}! &#9728;&#65039;</h2><p style={{ fontSize: 14, color: COLORS.textMuted, margin: 0 }}>{escolaNome}</p></div>
    <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12, marginBottom: 24 }}>{[{ label: "Planos pendentes", value: String(pendentes), icon: "&#128221;", bg: COLORS.yellowLight },{ label: "Avisos ativos", value: String(avisos.length), icon: "&#128226;", bg: COLORS.pinkLight },{ label: "Eventos", value: String(allEv.length), icon: "&#128197;", bg: COLORS.blueLight },{ label: "Equipe", value: String(turmas.length), icon: "&#128105;&#8205;&#127979;", bg: COLORS.greenLight }].map(function(s, i) { return (<div key={i} style={{ background: COLORS.white, borderRadius: 16, padding: 16, border: "1px solid " + COLORS.border }}><div style={{ width: 40, height: 40, borderRadius: 12, background: s.bg, display: "flex", alignItems: "center", justifyContent: "center", fontSize: 20, marginBottom: 10 }} dangerouslySetInnerHTML={{ __html: s.icon }} /><div style={{ fontSize: 24, fontWeight: 700, color: COLORS.text }}>{s.value}</div><div style={{ fontSize: 12, color: COLORS.textMuted, marginTop: 2 }}>{s.label}</div></div>); })}</div>
    <div style={{ background: COLORS.white, borderRadius: 16, padding: 16, border: "1px solid " + COLORS.border, marginBottom: 16 }}><h3 style={{ fontSize: 16, fontWeight: 700, color: COLORS.text, margin: "0 0 12px" }}>Planos recentes</h3>{planos.slice(0, 3).map(function(p: any) { return (<div key={p.id} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "10px 0", borderTop: "1px solid " + COLORS.border }}><div><div style={{ fontSize: 14, fontWeight: 600, color: COLORS.text }}>{p.titulo}</div><div style={{ fontSize: 12, color: COLORS.textMuted }}>{p.turmas?.nome}</div></div><StatusBadge status={p.status} /></div>); })}</div>
    <div style={{ background: COLORS.white, borderRadius: 16, padding: 16, border: "1px solid " + COLORS.border }}><h3 style={{ fontSize: 16, fontWeight: 700, color: COLORS.text, margin: "0 0 12px" }}>Agenda</h3>{allEv.slice(0, 4).map(function(ev: any) { return (<div key={ev.id} style={{ display: "flex", alignItems: "center", gap: 12, padding: "10px 0", borderTop: "1px solid " + COLORS.border }}><div style={{ width: 4, height: 36, borderRadius: 4, background: ev.cor, flexShrink: 0 }} /><div><div style={{ fontSize: 14, fontWeight: 600, color: COLORS.text }}>{ev.titulo}</div><div style={{ fontSize: 12, color: COLORS.textMuted }}>{new Date(ev.data + "T12:00:00").toLocaleDateString("pt-BR")}</div></div></div>); })}</div>
  </div>);
}

function MuralScreen({ avisos, user, usuarios, destinatarios, onAvisoCreated, onAvisoUpdated, onAvisoDeleted }: any) {
  var _a = useState(avisos); var lista = _a[0]; var setLista = _a[1]; var _b = useState(false); var sf = _b[0]; var setSf = _b[1]; var _c = useState(""); var tit = _c[0]; var setTit = _c[1]; var _d = useState(false); var urg = _d[0]; var setUrg = _d[1]; var _e = useState(""); var de = _e[0]; var setDe = _e[1]; var _f = useState("todos"); var atr = _f[0]; var setAtr = _f[1]; var _g = useState<string[]>([]); var su = _g[0]; var setSu = _g[1]; var _h = useState(""); var msg = _h[0]; var setMsg = _h[1]; var _i = useState<any>(null); var ed = _i[0]; var setEd = _i[1]; var _j = useState(""); var et = _j[0]; var setEt = _j[1]; var _k = useState(""); var edt = _k[0]; var setEdt = _k[1]; var _l = useState(false); var eu = _l[0]; var setEu = _l[1]; var _m = useState("todos"); var ea = _m[0]; var setEa = _m[1]; var _n = useState<string[]>([]); var esu = _n[0]; var setEsu = _n[1]; var _o = useState<any>(null); var vw = _o[0]; var setVw = _o[1];
  var isEd = user.papel === "coordenadora" || user.papel === "diretora"; var ou = usuarios.filter(function(u: any) { return u.id !== user.id; });
  async function criar() { if (!tit.trim()) return; var d: any = { escola_id: user.escola_id, autor_id: user.id, titulo: tit, urgente: urg }; if (de) d.data_evento = de; var r = await supabase.from("avisos").insert(d).select().single(); if (r.data) { if (atr === "todos") await supabase.from("aviso_destinatarios").insert({ aviso_id: r.data.id, todos: true }); else { var ds = su.map(function(uid) { return { aviso_id: r.data.id, usuario_id: uid, todos: false }; }); if (ds.length > 0) await supabase.from("aviso_destinatarios").insert(ds); } setLista([r.data, ...lista]); setTit(""); setDe(""); setUrg(false); setAtr("todos"); setSu([]); setSf(false); setMsg("Aviso publicado!"); if (onAvisoCreated) onAvisoCreated(r.data); } }
  function startEd(a: any) { setEd(a); setEt(a.titulo); setEdt(a.data_evento || ""); setEu(a.urgente); var di = destinatarios.filter(function(d: any) { return d.aviso_id === a.id; }); if (di.some(function(d: any) { return d.todos; })) { setEa("todos"); setEsu([]); } else { setEa("selecionar"); setEsu(di.filter(function(d: any) { return d.usuario_id; }).map(function(d: any) { return d.usuario_id; })); } }
  async function salvar() { if (!ed || !et.trim()) return; var ud: any = { titulo: et, urgente: eu, data_evento: edt || null }; await supabase.from("avisos").update(ud).eq("id", ed.id); await supabase.from("aviso_destinatarios").delete().eq("aviso_id", ed.id); if (ea === "todos") await supabase.from("aviso_destinatarios").insert({ aviso_id: ed.id, todos: true }); else { var ds = esu.map(function(uid) { return { aviso_id: ed.id, usuario_id: uid, todos: false }; }); if (ds.length > 0) await supabase.from("aviso_destinatarios").insert(ds); } setLista(lista.map(function(a: any) { return a.id === ed.id ? Object.assign({}, a, ud) : a; })); setEd(null); setMsg("Atualizado!"); if (onAvisoUpdated) onAvisoUpdated(); }
  async function excluir() { if (!ed) return; await supabase.from("aviso_destinatarios").delete().eq("aviso_id", ed.id); await supabase.from("avisos").delete().eq("id", ed.id); setLista(lista.filter(function(a: any) { return a.id !== ed.id; })); setEd(null); setMsg("Excluido!"); if (onAvisoDeleted) onAvisoDeleted(); }
  function tog(uid: string, s: string[], ss: any) { if (s.includes(uid)) ss(s.filter(function(id) { return id !== uid; })); else ss([...s, uid]); }
  function US({ users, selected, onTog }: any) { return (<div style={{ marginBottom: 12, maxHeight: 120, overflowY: "auto", border: "1px solid " + COLORS.border, borderRadius: 10, padding: 8 }}>{users.map(function(u: any) { var s = selected.includes(u.id); return (<div key={u.id} onClick={function() { onTog(u.id); }} style={{ display: "flex", alignItems: "center", gap: 10, padding: "8px 4px", cursor: "pointer", borderBottom: "1px solid " + COLORS.border }}><div style={{ width: 22, height: 22, borderRadius: 6, border: "2px solid " + (s ? COLORS.green : COLORS.border), background: s ? COLORS.green : "transparent", display: "flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}>{s && <span style={{ color: COLORS.white, fontSize: 12, fontWeight: 700 }}>&#10003;</span>}</div><div><div style={{ fontSize: 14, fontWeight: 600, color: COLORS.text }}>{u.nome}</div><div style={{ fontSize: 11, color: COLORS.textMuted }}>{u.papel}</div></div></div>); })}</div>); }
  return (<div>
    {msg && <SuccessMessage message={msg} onClose={function() { setMsg(""); }} />}
    {vw && <ViewEventModal aviso={vw} destinatarios={destinatarios} usuarios={usuarios} onClose={function() { setVw(null); }} />}
    {ed && (<div style={{ position: "fixed", top: 0, left: 0, right: 0, bottom: 0, zIndex: 200, background: "rgba(0,0,0,0.4)", display: "flex", alignItems: "center", justifyContent: "center", padding: 24 }}><div style={{ background: COLORS.white, borderRadius: 20, padding: 20, maxWidth: 380, width: "100%", maxHeight: "85vh", overflowY: "auto" }}><h3 style={{ fontSize: 18, fontWeight: 700, color: COLORS.text, margin: "0 0 16px" }}>Editar</h3><input value={et} onChange={function(e) { setEt(e.target.value); }} style={{ width: "100%", padding: "10px 14px", borderRadius: 10, border: "2px solid " + COLORS.border, fontSize: 14, outline: "none", boxSizing: "border-box", marginBottom: 10 }} /><label style={{ fontSize: 13, fontWeight: 600, color: COLORS.text, display: "block", marginBottom: 6 }}>Data</label><input type="date" value={edt} onChange={function(e) { setEdt(e.target.value); }} style={{ width: "100%", padding: "10px 14px", borderRadius: 10, border: "2px solid " + COLORS.border, fontSize: 14, outline: "none", boxSizing: "border-box", marginBottom: 12 }} /><label style={{ fontSize: 13, fontWeight: 600, color: COLORS.text, display: "block", marginBottom: 8 }}>Atribuir a:</label><div style={{ display: "flex", gap: 8, marginBottom: 12 }}><button onClick={function() { setEa("todos"); setEsu([]); }} style={{ flex: 1, padding: "8px", borderRadius: 10, border: "none", background: ea === "todos" ? COLORS.blue : COLORS.blueLight, color: ea === "todos" ? COLORS.white : COLORS.blue, fontSize: 13, fontWeight: 600, cursor: "pointer" }}>Todos</button><button onClick={function() { setEa("selecionar"); }} style={{ flex: 1, padding: "8px", borderRadius: 10, border: "none", background: ea === "selecionar" ? COLORS.blue : COLORS.blueLight, color: ea === "selecionar" ? COLORS.white : COLORS.blue, fontSize: 13, fontWeight: 600, cursor: "pointer" }}>Selecionar</button></div>{ea === "selecionar" && <US users={ou} selected={esu} onTog={function(uid: string) { tog(uid, esu, setEsu); }} />}<div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 16 }}><div onClick={function() { setEu(!eu); }} style={{ width: 22, height: 22, borderRadius: 6, border: "2px solid " + (eu ? COLORS.pink : COLORS.border), background: eu ? COLORS.pink : "transparent", display: "flex", alignItems: "center", justifyContent: "center", cursor: "pointer" }}>{eu && <span style={{ color: COLORS.white, fontSize: 12, fontWeight: 700 }}>&#10003;</span>}</div><span style={{ fontSize: 13, color: COLORS.text }}>Urgente</span></div><div style={{ display: "flex", gap: 8, marginBottom: 10 }}><button onClick={function() { setEd(null); }} style={{ flex: 1, padding: 10, borderRadius: 10, border: "2px solid " + COLORS.border, background: "transparent", color: COLORS.text, fontSize: 14, fontWeight: 600, cursor: "pointer" }}>Cancelar</button><button onClick={salvar} style={{ flex: 1, padding: 10, borderRadius: 10, border: "none", background: COLORS.blue, color: COLORS.white, fontSize: 14, fontWeight: 600, cursor: "pointer" }}>Salvar</button></div><button onClick={excluir} style={{ width: "100%", padding: 10, borderRadius: 10, border: "2px solid " + COLORS.pink, background: "transparent", color: COLORS.pink, fontSize: 14, fontWeight: 600, cursor: "pointer" }}>&#128465; Excluir</button></div></div>)}
    <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 20 }}><h2 style={{ fontSize: 20, fontWeight: 700, color: COLORS.text, margin: 0 }}>Mural</h2>{isEd && <button onClick={function() { setSf(!sf); }} style={{ background: COLORS.pink, color: COLORS.white, border: "none", borderRadius: 12, padding: "8px 16px", fontSize: 13, fontWeight: 600, cursor: "pointer" }}>{sf ? "X" : "+ Novo"}</button>}</div>
    {sf && (<div style={{ background: COLORS.white, borderRadius: 16, padding: 16, marginBottom: 16, border: "1px solid " + COLORS.border }}><input value={tit} onChange={function(e) { setTit(e.target.value); }} placeholder="Titulo..." style={{ width: "100%", padding: "10px 14px", borderRadius: 10, border: "2px solid " + COLORS.border, fontSize: 14, outline: "none", boxSizing: "border-box", marginBottom: 10 }} /><label style={{ fontSize: 13, fontWeight: 600, color: COLORS.text, display: "block", marginBottom: 6 }}>Data (opcional)</label><input type="date" value={de} onChange={function(e) { setDe(e.target.value); }} style={{ width: "100%", padding: "10px 14px", borderRadius: 10, border: "2px solid " + COLORS.border, fontSize: 14, outline: "none", boxSizing: "border-box", marginBottom: 12 }} /><label style={{ fontSize: 13, fontWeight: 600, color: COLORS.text, display: "block", marginBottom: 8 }}>Atribuir a:</label><div style={{ display: "flex", gap: 8, marginBottom: 12 }}><button onClick={function() { setAtr("todos"); setSu([]); }} style={{ flex: 1, padding: "8px", borderRadius: 10, border: "none", background: atr === "todos" ? COLORS.blue : COLORS.blueLight, color: atr === "todos" ? COLORS.white : COLORS.blue, fontSize: 13, fontWeight: 600, cursor: "pointer" }}>Todos</button><button onClick={function() { setAtr("selecionar"); }} style={{ flex: 1, padding: "8px", borderRadius: 10, border: "none", background: atr === "selecionar" ? COLORS.blue : COLORS.blueLight, color: atr === "selecionar" ? COLORS.white : COLORS.blue, fontSize: 13, fontWeight: 600, cursor: "pointer" }}>Selecionar</button></div>{atr === "selecionar" && <US users={ou} selected={su} onTog={function(uid: string) { tog(uid, su, setSu); }} />}<div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 12 }}><div onClick={function() { setUrg(!urg); }} style={{ width: 22, height: 22, borderRadius: 6, border: "2px solid " + (urg ? COLORS.pink : COLORS.border), background: urg ? COLORS.pink : "transparent", display: "flex", alignItems: "center", justifyContent: "center", cursor: "pointer" }}>{urg && <span style={{ color: COLORS.white, fontSize: 12, fontWeight: 700 }}>&#10003;</span>}</div><span style={{ fontSize: 13, color: COLORS.text }}>Urgente</span></div><button onClick={criar} style={{ width: "100%", padding: 10, borderRadius: 10, border: "none", background: COLORS.blue, color: COLORS.white, fontSize: 14, fontWeight: 600, cursor: "pointer" }}>Publicar</button></div>)}
    {lista.length === 0 && <p style={{ fontSize: 13, color: COLORS.textMuted, textAlign: "center", marginTop: 40 }}>Nenhum aviso</p>}
    {lista.map(function(av: any) { return (<div key={av.id} style={{ background: COLORS.white, borderRadius: 16, padding: 16, marginBottom: 12, border: "1px solid " + (av.urgente ? COLORS.pink + "33" : COLORS.border), borderLeft: av.urgente ? "4px solid " + COLORS.pink : "4px solid " + COLORS.border }}><h3 style={{ fontSize: 15, fontWeight: 600, color: COLORS.text, margin: "0 0 6px" }}>{av.urgente && <span style={{ color: COLORS.pink, marginRight: 6 }}>&#9679;</span>}{av.titulo}</h3><div style={{ display: "flex", gap: 8, flexWrap: "wrap", alignItems: "center", marginBottom: 10 }}><span style={{ fontSize: 12, color: COLORS.textMuted }}>{new Date(av.created_at).toLocaleDateString("pt-BR")}</span>{av.data_evento && <span style={{ fontSize: 11, background: COLORS.blueLight, color: COLORS.blue, padding: "2px 8px", borderRadius: 6, fontWeight: 600 }}>&#128197; {new Date(av.data_evento + "T12:00:00").toLocaleDateString("pt-BR")}</span>}</div><div style={{ display: "flex", gap: 8 }}><button onClick={function() { setVw(av); }} style={{ flex: 1, padding: "6px 12px", borderRadius: 8, border: "1px solid " + COLORS.border, background: COLORS.white, fontSize: 12, fontWeight: 600, color: COLORS.blue, cursor: "pointer" }}>&#128065; Ver</button>{isEd && <button onClick={function() { startEd(av); }} style={{ padding: "6px 12px", borderRadius: 8, border: "none", background: COLORS.blueLight, fontSize: 12, fontWeight: 600, color: COLORS.blue, cursor: "pointer" }}>&#9998; Editar</button>}</div></div>); })}
  </div>);
}

function ChatScreen({ turmas, user, usuarios }: any) {
  var _a = useState<any>(null); var at = _a[0]; var setAt = _a[1]; var _b = useState<any[]>([]); var ms = _b[0]; var setMs = _b[1]; var _c = useState(""); var nm = _c[0]; var setNm = _c[1]; var _d = useState(false); var ld = _d[0]; var setLd = _d[1]; var _e = useState(false); var sn = _e[0]; var setSn = _e[1]; var sr = useRef<HTMLDivElement>(null);
  function gn(uid: string) { var u = usuarios.find(function(u2: any) { return u2.id === uid; }); return u ? u.nome : "?"; }
  function gi(uid: string) { var n = gn(uid); var p = n.split(" "); return p.length >= 2 ? p[0][0] + p[1][0] : n.substring(0, 2); }
  async function oc(t: any) { setAt(t); setLd(true); var r = await supabase.from("mensagens").select("*").eq("turma_id", t.id).order("created_at", { ascending: true }); setMs(r.data || []); setLd(false); }
  useEffect(function() { if (!at) return; var ch = supabase.channel("c-" + at.id).on("postgres_changes", { event: "INSERT", schema: "public", table: "mensagens", filter: "turma_id=eq." + at.id }, function(p: any) { setMs(function(prev) { if (prev.find(function(m) { return m.id === p.new.id; })) return prev; return [...prev, p.new]; }); }).subscribe(); return function() { supabase.removeChannel(ch); }; }, [at]);
  useEffect(function() { if (sr.current) sr.current.scrollTop = sr.current.scrollHeight; }, [ms]);
  async function send() { if (!nm.trim() || !at || sn) return; setSn(true); await supabase.from("mensagens").insert({ turma_id: at.id, autor_id: user.id, conteudo: nm.trim() }); setNm(""); setSn(false); }
  if (at) return (<div style={{ display: "flex", flexDirection: "column", height: "calc(100vh - 160px)" }}><div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 16 }}><button onClick={function() { setAt(null); setMs([]); }} style={{ width: 36, height: 36, borderRadius: 10, border: "1px solid " + COLORS.border, background: COLORS.white, fontSize: 16, cursor: "pointer", display: "flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}>&#9664;</button><div><h3 style={{ fontSize: 16, fontWeight: 700, color: COLORS.text, margin: 0 }}>{at.nome}</h3><div style={{ fontSize: 12, color: COLORS.textMuted }}>{at.usuarios?.nome ? "Prof. " + at.usuarios.nome : ""}</div></div></div><div ref={sr} style={{ flex: 1, overflowY: "auto", background: COLORS.white, borderRadius: 16, padding: 16, border: "1px solid " + COLORS.border, marginBottom: 12 }}>{ld && <p style={{ fontSize: 13, color: COLORS.textMuted, textAlign: "center" }}>Carregando...</p>}{!ld && ms.length === 0 && <p style={{ fontSize: 13, color: COLORS.textMuted, textAlign: "center", marginTop: 40 }}>Nenhuma mensagem.</p>}{ms.map(function(m: any) { var me = m.autor_id === user.id; var tm = new Date(m.created_at).toLocaleTimeString("pt-BR", { hour: "2-digit", minute: "2-digit" }); return (<div key={m.id} style={{ display: "flex", flexDirection: me ? "row-reverse" : "row", gap: 8, marginBottom: 12, alignItems: "flex-end" }}>{!me && <div style={{ width: 32, height: 32, borderRadius: 10, background: COLORS.blueLight, display: "flex", alignItems: "center", justifyContent: "center", fontSize: 11, fontWeight: 700, color: COLORS.blue, flexShrink: 0 }}>{gi(m.autor_id)}</div>}<div style={{ maxWidth: "75%" }}>{!me && <div style={{ fontSize: 11, fontWeight: 600, color: COLORS.blue, marginBottom: 2, paddingLeft: 4 }}>{gn(m.autor_id)}</div>}<div style={{ background: me ? COLORS.blue : COLORS.bg, color: me ? COLORS.white : COLORS.text, padding: "10px 14px", borderRadius: me ? "16px 16px 4px 16px" : "16px 16px 16px 4px", fontSize: 14, lineHeight: "1.4" }}>{m.conteudo}</div><div style={{ fontSize: 10, color: COLORS.textMuted, marginTop: 2, textAlign: me ? "right" : "left", padding: "0 4px" }}>{tm}</div></div></div>); })}</div><div style={{ display: "flex", gap: 8 }}><input value={nm} onChange={function(e) { setNm(e.target.value); }} onKeyDown={function(e) { if (e.key === "Enter" && !e.shiftKey) { e.preventDefault(); send(); } }} placeholder="Mensagem..." style={{ flex: 1, padding: "12px 16px", borderRadius: 14, border: "2px solid " + COLORS.border, fontSize: 14, outline: "none", boxSizing: "border-box" }} /><button onClick={send} disabled={sn || !nm.trim()} style={{ width: 48, height: 48, borderRadius: 14, border: "none", background: COLORS.blue, color: COLORS.white, fontSize: 20, cursor: "pointer", display: "flex", alignItems: "center", justifyContent: "center", opacity: sn || !nm.trim() ? 0.5 : 1, flexShrink: 0 }}>&#10148;</button></div></div>);
  return (<div><h2 style={{ fontSize: 20, fontWeight: 700, color: COLORS.text, margin: "0 0 20px" }}>Chat por Turma</h2>{turmas.map(function(t: any) { return (<div key={t.id} onClick={function() { oc(t); }} style={{ background: COLORS.white, borderRadius: 16, padding: 16, marginBottom: 12, border: "1px solid " + COLORS.border, cursor: "pointer" }}><div style={{ display: "flex", alignItems: "center", gap: 12 }}><div style={{ width: 48, height: 48, borderRadius: 14, background: COLORS.blueLight, display: "flex", alignItems: "center", justifyContent: "center", fontSize: 22, flexShrink: 0 }}>{t.nome.includes("Ber") ? "&#127868;" : t.nome.includes("Maternal") ? "&#129528;" : "&#127912;"}</div><div style={{ flex: 1 }}><h3 style={{ fontSize: 15, fontWeight: 600, color: COLORS.text, margin: 0 }}>{t.nome}</h3><div style={{ fontSize: 13, color: COLORS.textMuted }}>{t.usuarios?.nome ? "Prof. " + t.usuarios.nome : ""} - {t.qtd_alunos} alunos</div></div><div style={{ width: 32, height: 32, borderRadius: 8, background: COLORS.blueLight, display: "flex", alignItems: "center", justifyContent: "center", fontSize: 14, color: COLORS.blue }}>&#9654;</div></div></div>); })}</div>);
}

function PlanosScreen({ planos: init }: any) {
  var _a = useState(init); var pl = _a[0]; var setPl = _a[1]; var _b = useState("todos"); var fi = _b[0]; var setFi = _b[1]; var _c = useState<string | null>(null); var fbi = _c[0]; var setFbi = _c[1]; var _d = useState(""); var fbt = _d[0]; var setFbt = _d[1];
  var filt = fi === "todos" ? pl : pl.filter(function(p: any) { return p.status === fi; });
  async function upd(id: string, st: string, fb?: string) { var d: any = { status: st }; if (fb) d.feedback = fb; await supabase.from("planos").update(d).eq("id", id); setPl(pl.map(function(p: any) { return p.id === id ? Object.assign({}, p, d) : p; })); setFbi(null); setFbt(""); }
  return (<div><h2 style={{ fontSize: 20, fontWeight: 700, color: COLORS.text, margin: "0 0 16px" }}>Planos de Aula</h2><div style={{ display: "flex", gap: 8, marginBottom: 20, overflowX: "auto" }}>{[{ k: "todos", l: "Todos" },{ k: "pendente", l: "Pendentes" },{ k: "aprovado", l: "Aprovados" },{ k: "revisar", l: "Revisar" }].map(function(f) { return <button key={f.k} onClick={function() { setFi(f.k); }} style={{ padding: "8px 16px", borderRadius: 20, border: "none", background: fi === f.k ? COLORS.blue : COLORS.blueLight, color: fi === f.k ? COLORS.white : COLORS.blue, fontSize: 13, fontWeight: 600, cursor: "pointer", whiteSpace: "nowrap" }}>{f.l}</button>; })}</div>{filt.length === 0 && <p style={{ fontSize: 13, color: COLORS.textMuted, textAlign: "center", marginTop: 40 }}>Nenhum plano</p>}{filt.map(function(p: any) { return (<div key={p.id} style={{ background: COLORS.white, borderRadius: 16, padding: 16, marginBottom: 12, border: "1px solid " + COLORS.border }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 8 }}><div><h3 style={{ fontSize: 15, fontWeight: 600, color: COLORS.text, margin: "0 0 2px" }}>{p.titulo}</h3><span style={{ fontSize: 13, color: COLORS.textMuted }}>{p.turmas?.nome}</span></div><StatusBadge status={p.status} /></div>{p.feedback && p.status === "revisar" && <div style={{ background: COLORS.yellowLight, borderRadius: 10, padding: "8px 12px", marginTop: 8 }}><div style={{ fontSize: 11, fontWeight: 600, color: "#B8860B", marginBottom: 2 }}>Ajustes:</div><div style={{ fontSize: 13, color: COLORS.text }}>{p.feedback}</div></div>}{p.status === "pendente" && (<div>{fbi === p.id ? (<div style={{ marginTop: 12 }}><textarea value={fbt} onChange={function(e) { setFbt(e.target.value); }} placeholder="Descreva os ajustes..." style={{ width: "100%", minHeight: 70, padding: 10, borderRadius: 10, border: "2px solid " + COLORS.yellow, fontSize: 13, outline: "none", boxSizing: "border-box", marginBottom: 8, resize: "vertical" }} /><div style={{ display: "flex", gap: 8 }}><button onClick={function() { setFbi(null); setFbt(""); }} style={{ flex: 1, padding: "8px", borderRadius: 10, border: "2px solid " + COLORS.border, background: "transparent", color: COLORS.text, fontSize: 13, fontWeight: 600, cursor: "pointer" }}>Cancelar</button><button onClick={function() { upd(p.id, "revisar", fbt); }} style={{ flex: 1, padding: "8px", borderRadius: 10, border: "none", background: COLORS.yellow, color: COLORS.white, fontSize: 13, fontWeight: 600, cursor: "pointer" }}>Enviar</button></div></div>) : (<div style={{ display: "flex", gap: 8, marginTop: 12 }}><button onClick={function() { upd(p.id, "aprovado"); }} style={{ flex: 1, padding: "8px", borderRadius: 10, border: "none", background: COLORS.green, color: COLORS.white, fontSize: 13, fontWeight: 600, cursor: "pointer" }}>&#10003; Aprovar</button><button onClick={function() { setFbi(p.id); }} style={{ flex: 1, padding: "8px", borderRadius: 10, border: "2px solid " + COLORS.yellow, background: "transparent", color: "#B8860B", fontSize: 13, fontWeight: 600, cursor: "pointer" }}>&#8635; Ajuste</button></div>)}</div>)}</div>); })}</div>);
}

function CalendarioScreen({ eventos, avisos }: any) {
  var today = new Date(); var _a = useState(today.getMonth()); var cm = _a[0]; var setCm = _a[1]; var _b = useState(today.getFullYear()); var cy = _b[0]; var setCy = _b[1];
  var all = [...eventos, ...avisos.filter(function(a: any) { return a.data_evento; }).map(function(a: any) { return { id: "a-" + a.id, titulo: a.titulo, data: a.data_evento, cor: a.urgente ? COLORS.pink : COLORS.blue, fromMural: true }; })].sort(function(a: any, b: any) { return a.data > b.data ? 1 : -1; });
  var dates = all.map(function(e: any) { return e.data; }); var dim = new Date(cy, cm + 1, 0).getDate(); var fd = new Date(cy, cm, 1).getDay();
  var mn = ["Janeiro","Fevereiro","Marco","Abril","Maio","Junho","Julho","Agosto","Setembro","Outubro","Novembro","Dezembro"];
  function prev() { if (cm === 0) { setCm(11); setCy(cy - 1); } else setCm(cm - 1); } function next() { if (cm === 11) { setCm(0); setCy(cy + 1); } else setCm(cm + 1); }
  var mp = cy + "-" + String(cm + 1).padStart(2, "0"); var me = all.filter(function(e: any) { return e.data.startsWith(mp); });
  return (<div><h2 style={{ fontSize: 20, fontWeight: 700, color: COLORS.text, margin: "0 0 20px" }}>Calendario</h2><div style={{ background: COLORS.white, borderRadius: 16, padding: 16, border: "1px solid " + COLORS.border, marginBottom: 20 }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 16 }}><button onClick={prev} style={{ width: 36, height: 36, borderRadius: 10, border: "1px solid " + COLORS.border, background: COLORS.white, fontSize: 18, cursor: "pointer", display: "flex", alignItems: "center", justifyContent: "center" }}>&#9664;</button><h3 style={{ fontSize: 16, fontWeight: 700, color: COLORS.text, margin: 0 }}>{mn[cm]} {cy}</h3><button onClick={next} style={{ width: 36, height: 36, borderRadius: 10, border: "1px solid " + COLORS.border, background: COLORS.white, fontSize: 18, cursor: "pointer", display: "flex", alignItems: "center", justifyContent: "center" }}>&#9654;</button></div><div style={{ display: "grid", gridTemplateColumns: "repeat(7, 1fr)", gap: 4, textAlign: "center" }}>{["D","S","T","Q","Q","S","S"].map(function(d, i) { return <div key={i} style={{ fontSize: 11, color: COLORS.textMuted, fontWeight: 600, padding: 4 }}>{d}</div>; })}{Array.from({ length: fd }, function(_, i) { return <div key={"e" + i} />; })}{Array.from({ length: dim }, function(_, i) { var day = i + 1; var ds = cy + "-" + String(cm + 1).padStart(2, "0") + "-" + String(day).padStart(2, "0"); var he = dates.includes(ds); var it = day === today.getDate() && cm === today.getMonth() && cy === today.getFullYear(); return <div key={day} style={{ padding: "6px 2px", borderRadius: 8, fontSize: 13, fontWeight: it ? 700 : 400, background: it ? COLORS.blue : he ? COLORS.pinkLight : "transparent", color: it ? COLORS.white : he ? COLORS.pink : COLORS.text }}>{day}</div>; })}</div></div><h3 style={{ fontSize: 16, fontWeight: 700, color: COLORS.text, margin: "0 0 12px" }}>Eventos de {mn[cm]}</h3>{me.length === 0 && <p style={{ fontSize: 13, color: COLORS.textMuted }}>Nenhum evento</p>}{me.map(function(ev: any) { return (<div key={ev.id} style={{ background: COLORS.white, borderRadius: 14, padding: 14, marginBottom: 10, display: "flex", alignItems: "center", gap: 12, border: "1px solid " + COLORS.border }}><div style={{ width: 44, height: 44, borderRadius: 12, background: ev.cor + "18", display: "flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}><div style={{ width: 10, height: 10, borderRadius: "50%", background: ev.cor }} /></div><div style={{ flex: 1 }}><div style={{ fontSize: 14, fontWeight: 600, color: COLORS.text }}>{ev.titulo}</div><div style={{ display: "flex", gap: 6, alignItems: "center" }}><span style={{ fontSize: 12, color: COLORS.textMuted }}>{new Date(ev.data + "T12:00:00").toLocaleDateString("pt-BR")}</span>{ev.fromMural && <span style={{ fontSize: 10, background: COLORS.yellowLight, color: "#B8860B", padding: "1px 6px", borderRadius: 4, fontWeight: 600 }}>Mural</span>}</div></div></div>); })}</div>);
}

function ChecklistScreen() { var _a = useState([{ id: 1, text: "Rotina organizada", checked: true },{ id: 2, text: "Materiais acessiveis", checked: true },{ id: 3, text: "Interacao positiva", checked: false },{ id: 4, text: "Espaco seguro", checked: true },{ id: 5, text: "Atividade alinhada", checked: false },{ id: 6, text: "Registro fotografico", checked: false }]); var it = _a[0]; var setIt = _a[1]; return (<div><h2 style={{ fontSize: 20, fontWeight: 700, color: COLORS.text, margin: "0 0 20px" }}>Checklist</h2><div style={{ background: COLORS.white, borderRadius: 16, padding: 16, border: "1px solid " + COLORS.border }}><div style={{ display: "flex", justifyContent: "space-between", marginBottom: 12 }}><div><h3 style={{ fontSize: 15, fontWeight: 600, color: COLORS.text, margin: "0 0 2px" }}>Visita - Bercario II</h3><span style={{ fontSize: 12, color: COLORS.textMuted }}>24/03/2026</span></div><div style={{ width: 44, height: 44, borderRadius: 12, background: COLORS.blueLight, display: "flex", alignItems: "center", justifyContent: "center", fontSize: 14, fontWeight: 700, color: COLORS.blue }}>{it.filter(function(i) { return i.checked; }).length}/{it.length}</div></div>{it.map(function(item) { return (<div key={item.id} onClick={function() { setIt(it.map(function(i) { return i.id === item.id ? Object.assign({}, i, { checked: !i.checked }) : i; })); }} style={{ display: "flex", alignItems: "center", gap: 12, padding: "12px 0", borderTop: "1px solid " + COLORS.border, cursor: "pointer" }}><div style={{ width: 24, height: 24, borderRadius: 8, border: "2px solid " + (item.checked ? COLORS.green : COLORS.border), background: item.checked ? COLORS.green : "transparent", display: "flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}>{item.checked && <span style={{ color: COLORS.white, fontSize: 14, fontWeight: 700 }}>&#10003;</span>}</div><span style={{ fontSize: 14, color: item.checked ? COLORS.textMuted : COLORS.text, textDecoration: item.checked ? "line-through" : "none" }}>{item.text}</span></div>); })}</div></div>); }

function GaleriaScreen({ user, turmas }: any) {
  var _a = useState<any[]>([]); var fotos = _a[0]; var setFotos = _a[1]; var _b = useState(false); var ld = _b[0]; var setLd = _b[1]; var _c = useState(false); var su = _c[0]; var setSu = _c[1]; var _d = useState("todas"); var fi = _d[0]; var setFi = _d[1]; var _e = useState<File | null>(null); var sf = _e[0]; var setSf = _e[1]; var _f = useState(""); var desc = _f[0]; var setDesc = _f[1]; var _g = useState(""); var ti = _g[0]; var setTi = _g[1]; var _h = useState(false); var up = _h[0]; var setUp = _h[1]; var _i = useState(""); var msg = _i[0]; var setMsg = _i[1]; var _j = useState<any>(null); var vf = _j[0]; var setVf = _j[1]; var _k = useState<string | null>(null); var pv = _k[0]; var setPv = _k[1]; var fr = useRef<HTMLInputElement>(null);
  useEffect(function() { setLd(true); supabase.from("fotos").select("*, turmas(nome)").order("created_at", { ascending: false }).then(function(r) { setFotos(r.data || []); setLd(false); }); }, []);
  function hfs(e: any) { var f = e.target.files?.[0]; if (f) { setSf(f); var r = new FileReader(); r.onload = function(ev) { setPv(ev.target?.result as string); }; r.readAsDataURL(f); } }
  async function upload() { if (!sf || !ti || up) return; setUp(true); var fn = user.id + "-" + Date.now() + "." + (sf.name.split(".").pop() || "jpg"); var ur = await supabase.storage.from("fotos").upload(fn, sf); if (ur.error) { setUp(false); return; } var pu = supabase.storage.from("fotos").getPublicUrl(fn).data.publicUrl; var ir = await supabase.from("fotos").insert({ turma_id: ti, autor_id: user.id, url: pu, descricao: desc || "Foto" }).select("*, turmas(nome)").single(); if (ir.data) { setFotos([ir.data, ...fotos]); setSf(null); setPv(null); setDesc(""); setTi(""); setSu(false); setMsg("Foto enviada!"); } setUp(false); }
  async function del(foto: any) { var p = foto.url.split("/"); await supabase.storage.from("fotos").remove([p[p.length - 1]]); await supabase.from("fotos").delete().eq("id", foto.id); setFotos(fotos.filter(function(f: any) { return f.id !== foto.id; })); setVf(null); setMsg("Foto excluida!"); }
  var ff = fi === "todas" ? fotos : fotos.filter(function(f: any) { return f.turma_id === fi; });
  var tn = ["todas"].concat(turmas.map(function(t: any) { return t.id; })); var tl: Record<string, string> = { todas: "Todas" }; turmas.forEach(function(t: any) { tl[t.id] = t.nome; });
  return (<div>
    {msg && <SuccessMessage message={msg} onClose={function() { setMsg(""); }} />}
    {vf && (<div style={{ position: "fixed", top: 0, left: 0, right: 0, bottom: 0, zIndex: 200, background: "rgba(0,0,0,0.9)", display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", padding: 20 }}><div style={{ position: "absolute", top: 16, right: 16, display: "flex", gap: 8 }}><button onClick={function() { del(vf); }} style={{ padding: "8px 16px", borderRadius: 10, border: "none", background: COLORS.pink, color: COLORS.white, fontSize: 13, fontWeight: 600, cursor: "pointer" }}>&#128465; Excluir</button><button onClick={function() { setVf(null); }} style={{ padding: "8px 16px", borderRadius: 10, border: "none", background: COLORS.white, color: COLORS.text, fontSize: 13, fontWeight: 600, cursor: "pointer" }}>X</button></div><img src={vf.url} alt={vf.descricao} style={{ maxWidth: "100%", maxHeight: "70vh", borderRadius: 12, objectFit: "contain" }} /><div style={{ marginTop: 16, textAlign: "center" }}><div style={{ fontSize: 16, fontWeight: 600, color: COLORS.white }}>{vf.descricao}</div><div style={{ fontSize: 13, color: "rgba(255,255,255,0.6)", marginTop: 4 }}>{vf.turmas?.nome} - {new Date(vf.created_at).toLocaleDateString("pt-BR")}</div></div></div>)}
    <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 20 }}><h2 style={{ fontSize: 20, fontWeight: 700, color: COLORS.text, margin: 0 }}>Galeria</h2><button onClick={function() { setSu(!su); }} style={{ background: COLORS.pink, color: COLORS.white, border: "none", borderRadius: 12, padding: "8px 16px", fontSize: 13, fontWeight: 600, cursor: "pointer" }}>{su ? "X" : "&#128247; Upload"}</button></div>
    {su && (<div style={{ background: COLORS.white, borderRadius: 16, padding: 16, marginBottom: 16, border: "1px solid " + COLORS.border }}><input ref={fr} type="file" accept="image/*" onChange={hfs} style={{ display: "none" }} />{pv ? (<div style={{ marginBottom: 12 }}><img src={pv} alt="Preview" style={{ width: "100%", height: 180, objectFit: "cover", borderRadius: 12, marginBottom: 8 }} /><button onClick={function() { setSf(null); setPv(null); if (fr.current) fr.current.value = ""; }} style={{ fontSize: 12, color: COLORS.pink, background: "none", border: "none", cursor: "pointer", fontWeight: 600 }}>Remover</button></div>) : (<div onClick={function() { if (fr.current) fr.current.click(); }} style={{ width: "100%", height: 140, borderRadius: 12, border: "2px dashed " + COLORS.border, display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", cursor: "pointer", marginBottom: 12 }}><div style={{ fontSize: 36, marginBottom: 4 }}>&#128247;</div><div style={{ fontSize: 13, color: COLORS.textMuted }}>Toque para escolher</div></div>)}<input value={desc} onChange={function(e) { setDesc(e.target.value); }} placeholder="Descricao..." style={{ width: "100%", padding: "10px 14px", borderRadius: 10, border: "2px solid " + COLORS.border, fontSize: 14, outline: "none", boxSizing: "border-box", marginBottom: 10 }} /><select value={ti} onChange={function(e) { setTi(e.target.value); }} style={{ width: "100%", padding: "10px 14px", borderRadius: 10, border: "2px solid " + COLORS.border, fontSize: 14, outline: "none", boxSizing: "border-box", marginBottom: 12, background: COLORS.white }}><option value="">Turma</option>{turmas.map(function(t: any) { return <option key={t.id} value={t.id}>{t.nome}</option>; })}</select><button onClick={upload} disabled={up || !sf || !ti} style={{ width: "100%", padding: 10, borderRadius: 10, border: "none", background: COLORS.blue, color: COLORS.white, fontSize: 14, fontWeight: 600, cursor: "pointer", opacity: up || !sf || !ti ? 0.5 : 1 }}>{up ? "Enviando..." : "Enviar"}</button></div>)}
    <div style={{ display: "flex", gap: 8, marginBottom: 20, overflowX: "auto" }}>{tn.map(function(id) { return <button key={id} onClick={function() { setFi(id); }} style={{ padding: "8px 16px", borderRadius: 20, border: "none", background: fi === id ? COLORS.blue : COLORS.blueLight, color: fi === id ? COLORS.white : COLORS.blue, fontSize: 13, fontWeight: 600, cursor: "pointer", whiteSpace: "nowrap" }}>{tl[id] || id}</button>; })}</div>
    {ld && <p style={{ fontSize: 13, color: COLORS.textMuted, textAlign: "center" }}>Carregando...</p>}
    {!ld && ff.length === 0 && <p style={{ fontSize: 13, color: COLORS.textMuted, textAlign: "center", marginTop: 40 }}>Nenhuma foto</p>}
    <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10 }}>{ff.map(function(f: any) { return (<div key={f.id} onClick={function() { setVf(f); }} style={{ borderRadius: 14, overflow: "hidden", background: COLORS.white, border: "1px solid " + COLORS.border, cursor: "pointer" }}><img src={f.url} alt={f.descricao} style={{ width: "100%", height: 120, objectFit: "cover" }} /><div style={{ padding: 10 }}><div style={{ fontSize: 13, fontWeight: 600, color: COLORS.text }}>{f.descricao}</div><div style={{ fontSize: 11, color: COLORS.textMuted }}>{f.turmas?.nome}</div></div></div>); })}</div>
  </div>);
}

var TABS = [{ id: "dashboard", icon: "&#127968;", label: "Inicio" },{ id: "mural", icon: "&#128226;", label: "Mural" },{ id: "chat", icon: "&#128172;", label: "Chat" },{ id: "planos", icon: "&#128221;", label: "Planos" },{ id: "calendario", icon: "&#128197;", label: "Agenda" },{ id: "checklist", icon: "&#9989;", label: "Visitas" },{ id: "galeria", icon: "&#128247;", label: "Fotos" }];

export default function Home() {
  var _a = useState<any>(null); var user = _a[0]; var setUser = _a[1]; var _b = useState("dashboard"); var tab = _b[0]; var setTab = _b[1]; var _c = useState<any[]>([]); var avisos = _c[0]; var setAvisos = _c[1]; var _d = useState<any[]>([]); var planos = _d[0]; var setPlanos = _d[1]; var _e = useState<any[]>([]); var eventos = _e[0]; var setEventos = _e[1]; var _f = useState<any[]>([]); var turmas = _f[0]; var setTurmas = _f[1]; var _g = useState<any[]>([]); var usuarios = _g[0]; var setUsuarios = _g[1]; var _h = useState<any[]>([]); var dest = _h[0]; var setDest = _h[1]; var _i = useState(false); var ld = _i[0]; var setLd = _i[1]; var _j = useState(false); var showMenu = _j[0]; var setShowMenu = _j[1];

  async function loadData(ud: any) { setLd(true); var r = await Promise.all([supabase.from("avisos").select("*").eq("escola_id", ud.escola_id).order("created_at", { ascending: false }), supabase.from("planos").select("*, turmas(nome)").order("created_at", { ascending: false }), supabase.from("eventos").select("*").eq("escola_id", ud.escola_id).order("data", { ascending: true }), supabase.from("turmas").select("*, usuarios(nome)").eq("escola_id", ud.escola_id), supabase.from("usuarios").select("*").eq("escola_id", ud.escola_id), supabase.from("aviso_destinatarios").select("*")]); setAvisos(r[0].data || []); setPlanos(r[1].data || []); setEventos(r[2].data || []); setTurmas(r[3].data || []); setUsuarios(r[4].data || []); setDest(r[5].data || []); setLd(false); }
  function handleLogin(ud: any) { setUser(ud); loadData(ud); }
  function onAC(a: any) { setAvisos(function(p) { if (p.find(function(x) { return x.id === a.id; })) return p; return [a, ...p]; }); }
  function rDest() { supabase.from("aviso_destinatarios").select("*").then(function(r) { setDest(r.data || []); }); }
  var isEditor = user && (user.papel === "coordenadora" || user.papel === "diretora");

  if (!user) return <LoginScreen onLogin={handleLogin} />;
  if (ld) return (<div style={{ minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center", background: COLORS.bg }}><div style={{ textAlign: "center" }}><div style={{ fontSize: 40, marginBottom: 12 }}>&#128203;</div><p style={{ color: COLORS.textMuted, fontSize: 14 }}>Carregando...</p></div></div>);

  return (
    <div style={{ background: COLORS.bg, minHeight: "100vh", maxWidth: 480, margin: "0 auto", position: "relative" }}>
      {showMenu && (<div style={{ position: "fixed", top: 0, left: 0, right: 0, bottom: 0, zIndex: 100, background: "rgba(0,0,0,0.3)" }} onClick={function() { setShowMenu(false); }} />)}
      <div style={{ position: "sticky", top: 0, zIndex: 150, background: COLORS.white, borderBottom: "1px solid " + COLORS.border, padding: "12px 20px", display: "flex", justifyContent: "space-between", alignItems: "center" }}>
        <div style={{ display: "flex", alignItems: "center", gap: 10 }}><div style={{ width: 36, height: 36, borderRadius: 10, background: "linear-gradient(135deg, " + COLORS.blue + ", " + COLORS.blueDark + ")", display: "flex", alignItems: "center", justifyContent: "center" }}><span style={{ color: COLORS.white, fontSize: 16 }}>&#128203;</span></div><div><div style={{ fontSize: 15, fontWeight: 700, color: COLORS.text }}>Coordinator</div><div style={{ fontSize: 10, color: COLORS.textMuted, letterSpacing: 1, textTransform: "uppercase" }}>by KidFly</div></div></div>
        <div style={{ position: "relative" }}>
          <div onClick={function() { setShowMenu(!showMenu); }} style={{ width: 36, height: 36, borderRadius: 10, background: COLORS.pinkLight, display: "flex", alignItems: "center", justifyContent: "center", fontSize: 18, cursor: "pointer" }}>&#128100;</div>
          {showMenu && (<div style={{ position: "absolute", top: 44, right: 0, background: COLORS.white, borderRadius: 12, boxShadow: "0 8px 30px rgba(0,0,0,0.15)", border: "1px solid " + COLORS.border, overflow: "hidden", minWidth: 180, zIndex: 200 }}>
            <div style={{ padding: "12px 16px", borderBottom: "1px solid " + COLORS.border }}><div style={{ fontSize: 14, fontWeight: 600, color: COLORS.text }}>{user.nome}</div><div style={{ fontSize: 12, color: COLORS.textMuted }}>{user.papel}</div></div>
            {isEditor && <div onClick={function() { setTab("gestao"); setShowMenu(false); }} style={{ padding: "12px 16px", cursor: "pointer", fontSize: 14, color: COLORS.blue, fontWeight: 600, borderBottom: "1px solid " + COLORS.border }}>&#9881; Gerenciar escola</div>}
            <div onClick={function() { setUser(null); setTab("dashboard"); setShowMenu(false); }} style={{ padding: "12px 16px", cursor: "pointer", fontSize: 14, color: COLORS.pink, fontWeight: 600 }}>&#10140; Sair</div>
          </div>)}
        </div>
      </div>
      <div style={{ padding: "20px 20px 100px" }}>
        {tab === "dashboard" && <DashboardScreen user={user} avisos={avisos} planos={planos} eventos={eventos} turmas={turmas} />}
        {tab === "mural" && <MuralScreen avisos={avisos} user={user} usuarios={usuarios} destinatarios={dest} onAvisoCreated={onAC} onAvisoUpdated={rDest} onAvisoDeleted={function() { loadData(user); }} />}
        {tab === "chat" && <ChatScreen turmas={turmas} user={user} usuarios={usuarios} />}
        {tab === "planos" && <PlanosScreen planos={planos} />}
        {tab === "calendario" && <CalendarioScreen eventos={eventos} avisos={avisos} />}
        {tab === "checklist" && <ChecklistScreen />}
        {tab === "galeria" && <GaleriaScreen user={user} turmas={turmas} />}
        {tab === "gestao" && <GestaoScreen user={user} turmas={turmas} usuarios={usuarios} onUpdate={function() { loadData(user); }} />}
      </div>
      <div style={{ position: "fixed", bottom: 0, left: "50%", transform: "translateX(-50%)", width: "100%", maxWidth: 480, background: COLORS.white, borderTop: "1px solid " + COLORS.border, display: "flex", justifyContent: "space-around", padding: "6px 4px 20px", zIndex: 50, boxShadow: "0 -4px 20px rgba(0,0,0,0.05)" }}>
        {TABS.map(function(t) { return (<button key={t.id} onClick={function() { setTab(t.id); }} style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: 2, background: "none", border: "none", padding: "4px 6px", cursor: "pointer", opacity: tab === t.id ? 1 : 0.5 }}><span style={{ fontSize: 20, display: "block", transform: tab === t.id ? "scale(1.15)" : "scale(1)", transition: "transform 0.2s" }} dangerouslySetInnerHTML={{ __html: t.icon }} /><span style={{ fontSize: 10, fontWeight: tab === t.id ? 700 : 500, color: tab === t.id ? COLORS.blue : COLORS.textMuted }}>{t.label}</span>{tab === t.id && <div style={{ width: 4, height: 4, borderRadius: "50%", background: COLORS.pink, marginTop: 1 }} />}</button>); })}
      </div>
    </div>
  );
}
